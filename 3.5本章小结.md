# 3.5 本章小结

本章围绕低标注场景下的房颤检测问题，系统设计了一种基于深度学习的半监督检测方法。首先，从整体上构建了算法框架，将数据预处理、规则冷启动、双模型协同建模以及交互式伪标签迭代训练组织为统一流程，明确了各模块之间的逻辑关系与功能分工。

随后，针对初始标注样本不足的问题，本文设计了基于 RR 间期的规则检测与自动标注方法。该方法利用房颤节律不规则的医学先验，通过 R 波定位、RR 间期特征提取、双阈值规则判别以及置信度筛选，构建出高质量的初始伪标签训练集，为后续深度模型训练提供了可靠起点。

在此基础上，本文进一步设计了由 CNN 和 LSTM 构成的双模型协同检测结构。其中，CNN 负责提取 ECG 信号的局部形态特征，LSTM 负责建模节律变化的时序依赖关系。两类模型在特征表达上具有明显互补性，使得算法能够从不同角度对房颤特征进行建模，为后续交互学习奠定了模型基础。

最后，围绕未标注数据的有效利用问题，本文提出了交互式伪标签迭代训练策略。该策略通过高置信度样本筛选、类别平衡控制、双模型交叉馈送和多轮动态更新，使未标注样本能够逐步转化为有效监督信息，从而不断提升模型性能。与传统单模型自训练方法相比，本文方法更加突出异构模型之间的协同作用，能够在有限标注条件下实现更稳定的训练过程和更高的数据利用效率。

综上所述，本章完成了本文房颤检测算法的整体设计，形成了“规则方法提供冷启动、双模型结构负责特征学习、交互式迭代策略实现持续优化”的完整技术路线。这一设计不仅为后续实验部分提供了方法基础，也为验证低标注场景下房颤自动检测的有效性提供了理论和实现支撑。