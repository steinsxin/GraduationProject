# 第5章 结论

本文围绕低标注场景下的房颤自动检测问题，针对传统监督学习对大规模标注数据依赖较强、单模型特征表达能力有限以及未标注数据利用不足等问题，提出了一种基于深度学习的双模型半监督房颤检测方法。全文在理论分析、方法设计和实验验证三个层面展开研究，形成了较为完整的技术路线，并取得了以下几方面成果。

首先，在方法设计方面，本文构建了面向低标注场景的房颤检测整体框架。该框架以少量真实标签和大量未标注样本为基础，将规则冷启动、双模型协同建模和交互式伪标签迭代训练有机结合起来，形成了从初始伪标签生成到深度模型持续优化的完整流程。与传统仅依赖单一监督数据训练的方法相比，本文方法能够更充分地挖掘未标注数据中的有效信息，提高模型在低标注条件下的学习能力。

其次，在初始样本构建方面，本文设计了基于 RR 间期的规则检测与自动标注方法。该方法利用房颤节律不规则这一医学先验，通过信号预处理、R 波定位、RR 间期特征提取、双阈值规则判别和置信度筛选，为后续深度学习模型提供了高质量的初始伪标签训练集。该部分研究解决了半监督学习中“如何获得可靠冷启动样本”的关键问题，为后续模型训练奠定了基础。

再次，在模型结构设计方面，本文构建了由 CNN 和 LSTM 组成的双模型协同检测结构。其中，CNN 侧重于学习 ECG 信号的局部形态特征，LSTM 侧重于建模节律变化的时序依赖关系，两类模型在特征表达上具有明显互补性。基于这种互补关系，本文进一步提出了交互式伪标签迭代训练策略，使两个模型能够在多轮训练中相互提供高置信度样本，不断更新训练集并提升整体检测性能。

最后，在实验验证方面，本文分别在 CPSC2025 数据集和 MIT-BIH 数据集上开展了实验。实验结果表明，本文提出的双模型半监督学习方法在 CPSC2025 数据集上取得了优于单模型监督学习方法的检测性能，其中融合模型表现最佳，说明双模型协同机制能够有效提升房颤检测效果。同时，在 MIT-BIH 数据集上的辅助验证实验进一步表明，交互式伪标签迭代训练策略能够对主分支模型产生正向促进作用，验证了该策略具有一定的有效性和可迁移性。

综合全文研究可以得出以下结论：本文提出的“规则冷启动 + 双模型协同建模 + 交互式伪标签迭代训练”技术路线，能够较好地解决低标注场景下房颤检测中训练样本不足和特征学习不充分的问题，具有较好的理论合理性和实验有效性。该方法不仅提升了房颤检测的准确率和综合识别能力，也为未标注 ECG 数据的利用提供了一种可行思路。

当然，本文工作仍存在进一步完善的空间。首先，双模型协同效果在一定程度上依赖于辅助模型本身的性能，当辅助模型判别能力不足时，模型融合和交互效果会受到限制。其次，当前实验轮次和参数设置仍以经验调整为主，后续可进一步引入更系统的参数优化策略。再次，本文对未标注样本的利用主要依赖高置信度筛选机制，未来还可以考虑结合更复杂的一致性约束、对比学习或注意力机制，以进一步提升伪标签质量和模型鲁棒性。

后续研究可从以下几个方向展开：一是继续优化 LSTM 或引入更适合长程时序建模的结构，以增强辅助分支的特征表达能力；二是改进双模型之间的交互方式，使样本馈送过程更加稳定和精细；三是在更多公开数据集和更复杂场景下开展实验，以进一步验证本文方法的泛化能力和实际应用价值。总体而言，本文工作为低标注条件下的房颤自动检测研究提供了新的思路，也为后续相关研究奠定了一定基础。