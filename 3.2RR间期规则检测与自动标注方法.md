# 3.2 RR间期的规则检测与自动标注方法

## 3.2.1 方法设计背景

在半监督学习框架中，模型训练通常需要一定数量的初始标注样本作为启动条件。然而在心电信号分析任务中，人工标注成本较高，尤其是在大规模 ECG 数据场景下，完全依赖人工标注并不现实。因此，本文在深度学习模型训练之前，先设计了一套基于 RR 间期统计规律的传统规则检测方法，用于对未标注样本进行初始自动标注。

该方法的基本思想是：房颤的一个典型特征是心室节律呈现明显的“绝对不规则”状态，这种不规则性会直接反映在相邻 R 波之间的时间间隔变化上。换言之，只要能够较准确地定位 ECG 信号中的 R 波位置，就可以通过 RR 间期序列的统计特征来判断当前片段更接近正常节律还是房颤节律。基于这一思路，本文构建了“信号预处理 - QRS 检测 - RR 间期特征提取 - 规则判别 - 置信度筛选”的自动标注流程。

## 3.2.2 整体流程

本文的 RR 间期规则检测与自动标注方法主要包括以下几个阶段：

1. 对原始 ECG 信号进行带通滤波，降低噪声和基线漂移对后续检测的影响。
2. 通过差分增强与峰值搜索定位 R 波，得到每个信号片段中的 R 峰位置。
3. 根据相邻 R 波位置计算 RR 间期序列，并提取用于描述节律不规则性的统计特征。
4. 将统计特征与预设阈值进行比较，完成正常类和房颤类的规则判别。
5. 根据样本与阈值的偏离程度计算置信度，筛选高置信度样本作为初始伪标签数据。

整体而言，这一方法不依赖深度模型参数学习，而是直接利用心电节律的医学先验完成初始样本自动标注，因此具有可解释性强、实现简单和启动成本低的优点。

## 3.2.3 基于 RR 间期的分类原理

RR 间期是指相邻两个 R 波峰值之间的时间间隔。由于 R 波通常对应心室去极化过程中最明显的尖峰，因此 RR 间期能够直接反映心跳节律的变化规律。

在正常窦性心律下，心跳节律总体较稳定，相邻 RR 间期虽然会存在一定波动，但整体变化较小，序列表现为相对平稳。而在房颤状态下，心房电活动紊乱，传导到心室的节律失去规律性，导致 RR 间期往往表现出较大的离散性和逐拍波动性。因此，通过分析 RR 间期序列的波动幅度和变化强度，可以对心律类型进行有效区分。

在你的代码实现中，RR 间期规则分类并不是直接用某一个单独指标完成判断，而是同时结合两个统计特征：

1. CV：反映 RR 间期整体离散程度。
2. ARI：反映相邻 RR 间期之间的变化剧烈程度。

这两个特征分别从“整体波动”和“局部变化”两个角度刻画节律不规则性，因此能够较好地用于房颤自动识别。

## 3.2.4 信号预处理

原始 ECG 信号中通常包含基线漂移、工频干扰和高频噪声，这些成分会直接影响 R 波定位精度。为此，本文首先对信号进行带通滤波处理。代码中采用的是三阶 Butterworth 带通滤波器，通带范围设置为 5 Hz 到 45 Hz。

这一频率范围的选择具有明确的生理意义：一方面，它能够保留 QRS 波群的主要能量成分；另一方面，又能抑制低频漂移和过高频率噪声，从而使 R 波特征更加突出。经过滤波后，信号波形更加平滑，为后续差分和峰值检测提供了更稳定的输入。

## 3.2.5 QRS 波检测与 R 波定位

R 波定位是 RR 间期计算的前提。如果 R 波检测不准确，那么后续得到的 RR 间期序列也会失真，最终影响分类效果。因此，在规则检测方法中，QRS 波检测是最关键的步骤之一。

### 1. 差分增强

在实现中，首先对滤波后的信号进行差分运算。这样做的原因在于，QRS 波群中的 R 波上升沿和下降沿通常最为陡峭，而差分操作能够显著增强这种斜率变化，使 R 波在处理后的信号中更容易被识别出来。

代码采用两点差分的方式近似描述局部变化趋势。经过该步骤后，平缓波形的响应会被削弱，而快速变化的尖峰结构会被强化，因此更利于后续峰值提取。

### 2. 自适应阈值构造

不同样本之间 ECG 振幅差异较大，如果使用固定阈值寻找 R 波，很容易出现某些信号检测过多、某些信号检测过少的问题。因此，代码没有采用固定阈值，而是根据当前信号本身的统计特性动态生成阈值。

具体来说，代码会先统计差分信号中正值部分的高分位幅度，再结合原始信号绝对值的幅度水平，取两者中的较大值作为检测阈值。这种做法的本质是让阈值随着信号强度变化而自适应调整，从而提高 R 波检测的稳定性。

### 3. 峰值搜索与最小间隔约束

在得到检测阈值后，算法使用峰值搜索方法寻找所有超过阈值的局部峰值。同时，为避免同一个 QRS 波群被重复检测，代码设置了最小峰距约束，即两个 R 波之间至少相隔一个最短生理时间窗口。该窗口长度按照采样率折算后约为 200 ms，能够有效抑制噪声尖峰或 T 波干扰带来的误检。

完成上述步骤后，算法便可得到当前 ECG 片段中的 R 波位置序列。这一序列是后续 RR 特征计算的基础。

## 3.2.6 RR 间期特征提取

在获得 R 波位置后，算法通过相邻 R 波索引之差并结合采样率，将其转换为以秒为单位的 RR 间期序列。若某段信号中检测到的 R 波数量过少，无法形成稳定的 RR 统计特征，则代码会直接将该样本的特征置为低置信度默认值，以避免异常样本对规则判别造成干扰。

本文采用以下两个特征描述节律不规则性。

### 1. CV 特征

CV 是 RR 间期序列的变异系数，其本质是标准差相对于均值的归一化表达。它反映的是一段时间内 RR 间期整体波动是否明显。

如果样本属于正常节律，则 RR 间期相对稳定，CV 往往较小；如果样本属于房颤节律，则 RR 间期长短变化明显，CV 通常较大。因此，CV 可以从宏观层面描述节律是否稳定。

### 2. ARI 特征

ARI 用于衡量相邻 RR 间期之间的变化程度。与 CV 更关注整体离散性不同，ARI 更强调逐拍之间的跳变幅度。

正常节律下，相邻 RR 间期变化通常较平滑，因此 ARI 值相对较低；而房颤节律下，相邻心拍间隔常常出现明显突变，因此 ARI 值更高。该特征能够从微观层面刻画节律混乱程度。

### 3. 双特征联合描述的意义

仅使用单一特征进行分类容易产生误判。例如，某些样本虽然整体波动较大，但逐拍变化并不剧烈；另一些样本可能存在局部突变，却不一定属于持续性房颤。为提高规则判别的可靠性，本文同时使用 CV 和 ARI 两个特征。

其中，CV 负责描述整体不稳定性，ARI 负责描述相邻心拍间的不规则变化。两者联合使用后，可以从不同层面更完整地刻画房颤节律特征，从而增强初始伪标签的可信度。

## 3.2.7 基于 RR 间期特征的规则分类方法

在得到 CV 和 ARI 两个特征后，本文通过预设阈值对样本进行规则分类。代码中设置：

1. CV 阈值为 0.02。
2. ARI 阈值为 0.24。

其分类逻辑非常明确：

1. 当 CV 大于阈值且 ARI 也大于阈值时，将该样本判定为房颤类。
2. 当上述条件不同时满足时，将该样本判定为正常类。

这意味着本文采用的是一种“双阈值联合判别”策略，而不是简单的单指标判别。其核心思想在于：只有当样本同时表现出较高的整体波动性和较高的逐拍变化强度时，才认为其具有较强的房颤特征。这样的规则可以降低由于单一特征异常而导致的误标概率。

从医学含义上看，这一判别逻辑也具有较好的可解释性。因为房颤不仅表现为 RR 间期整体不规则，还表现为相邻心拍之间缺乏稳定的节律模式。因此，CV 和 ARI 同时越过阈值时，样本更可能属于房颤；反之，则更接近正常节律或非典型异常节律。

## 3.2.8 自动标注中的置信度计算方法

为了让规则分类结果能够进一步用于后续模型训练，本文没有简单地将所有规则判别结果直接作为伪标签，而是引入了置信度机制，对样本可靠性进行再次评估。

在代码实现中，首先根据样本被判为房颤类还是正常类，分别计算其相对于阈值的偏离程度：

1. 如果样本被判为房颤类，则比较 CV 和 ARI 分别高出阈值的程度。
2. 如果样本被判为正常类，则比较 CV 和 ARI 分别低于阈值的程度。

随后，将两项偏离程度中较保守的一项作为主要评分依据，再映射到 50 到 100 的置信度区间。这样设计的原因在于：只有当两个特征都明显支持当前分类结果时，样本才能获得更高的置信度；若其中一个特征只是勉强越过或接近阈值，则整体置信度不会被评得过高。

这种置信度计算方式具有两个优点：

1. 保守性较强，可以减少边界样本被误认为高质量伪标签的风险。
2. 与规则分类逻辑保持一致，使高置信度样本往往对应更典型的节律模式。

因此，置信度不仅是一个辅助评分指标，也是在自动标注阶段控制伪标签质量的关键机制。

## 3.2.9 高置信度样本筛选与初始伪标签构建

在获得所有未标注样本的规则分类结果和置信度之后，算法并不会直接全部用于训练，而是进一步筛选出高质量样本。

在你的代码中，先将样本按规则判定结果分为房颤候选集和正常候选集，然后设置严格置信度阈值 85，仅保留高于该阈值的样本作为“合格伪标签样本”。这样做的目的，是优先保证初始训练集的标签纯度，而不是一味追求样本数量。

接着，考虑到房颤类和正常类样本数量可能不均衡，算法对两类样本进行平衡处理：

1. 分别统计两类中满足条件的样本数。
2. 取两类中较小的数量作为最终保留数量。
3. 在每一类内部按置信度从高到低排序，仅保留前若干个样本。

在代码实现中，还进一步设置了每类最多 2000 个样本的上限，以防止某一类样本数量过大影响初始训练集结构。经过筛选和平衡后，最终得到一组规模适中、类别均衡且标签可信度较高的初始伪标签数据集。该数据集随后作为 CNN 和 LSTM 交互式训练的起点。

## 3.2.10 该方法在整体框架中的作用

在你的整体算法设计中，基于 RR 间期的规则检测与自动标注方法主要承担“冷启动”作用。也就是说，它不是最终的分类器，而是为后续深度学习模型提供第一批可用的训练样本。

这一设计非常关键，因为深度模型虽然具有较强的特征学习能力，但如果完全没有初始监督信息，就难以直接进入有效训练状态。传统 RR 间期规则方法正好能够利用医学先验，为未标注样本生成一批高质量初始伪标签，从而使后续 CNN 和 LSTM 模型可以在此基础上继续进行迭代优化。

换言之，本文整体方法并不是“传统规则”和“深度学习模型”二选一的关系，而是先利用传统规则建立可靠起点，再利用深度学习模型从更复杂的波形特征中挖掘潜在规律，二者共同构成完整的半监督学习流程。

## 3.2.11 方法特点总结

综合来看，本文提出的 RR 间期规则检测与自动标注方法具有以下特点：

1. 直接利用房颤节律不规则这一医学先验，具有较强可解释性。
2. 通过滤波、差分增强和自适应阈值检测，提高了 R 波定位的稳定性。
3. 结合 CV 和 ARI 两个统计特征，实现了对整体波动性和局部变化性的联合建模。
4. 引入置信度评估和高置信度筛选机制，提高了自动标注结果的可靠性。
5. 通过类别平衡和 Top-K 保留策略，构建了适合后续模型训练的初始伪标签数据集。

因此，该方法不仅完成了未标注 ECG 样本的初步分类，更为整个交互式半监督学习框架提供了高质量的启动样本来源，是本文算法设计中的重要组成部分。