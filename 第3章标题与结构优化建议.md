# 第3章标题与结构优化建议

## 一、当前标题存在的问题

你现在的第 3 章结构总体思路是对的，但从论文写作角度看，3.3 和 3.4 的标题还可以再提升一下，主要有两个问题：

1. 3.3“面向低标注场景的深度学习模型”表述偏泛，读者看不出这一节到底是介绍单个模型结构、双模型协同机制，还是半监督框架本身。
2. 3.4“模型训练与优化策略”也比较宽泛，容易和实验部分、实现细节部分混在一起，不够突出你这篇论文的特色，也没有把“伪标签迭代训练”这个重点表现出来。

如果第 3 章的主题是“基于深度学习的房颤检测算法设计”，那么从 3.3 开始，标题应该更明确地体现出：

1. 模型结构设计。
2. 半监督或低标注学习机制。
3. 训练过程中的迭代优化策略。

## 二、推荐的第3章结构

建议你将第 3 章调整为如下结构：

### 第3章 基于深度学习的房颤检测算法设计

3.1 算法整体框架设计  
3.2 基于RR间期的规则检测与自动标注方法  
3.3 CNN-LSTM双模型协同检测模型设计  
3.4 交互式伪标签迭代训练策略  
3.5 本章小结

这一版的优点是：

1. 3.1 讲整体框架。
2. 3.2 讲传统规则冷启动。
3. 3.3 讲深度模型本体，即你到底设计了什么模型。
4. 3.4 讲训练机制，即模型是如何在低标注条件下不断优化的。
5. 层次清晰，逻辑上符合“先框架、后规则、再模型、最后训练策略”的论文写法。

## 三、重点推荐标题

如果你想尽量少改原目录，我最推荐你直接改成下面这一版：

1. 3.3 CNN-LSTM双模型协同检测模型设计
2. 3.4 交互式伪标签迭代训练策略

这是目前最贴合你代码实现的一组标题。

原因如下：

1. “CNN-LSTM双模型协同检测模型设计”能够准确对应你代码里的 CNN 和 LSTM 两个模型，以及两者在整体方法中的互补关系。
2. “交互式伪标签迭代训练策略”能够准确对应你代码中的 rule-based 冷启动、双模型互相提供高置信度样本、逐轮迭代更新训练集的核心思想。
3. 这两个标题组合起来，比“低标注场景的深度学习模型”“模型训练与优化策略”更聚焦，也更像算法章节而不是普通实现说明。

## 四、如果你想写得更学术一些

如果你希望标题更正式、更偏学术论文风格，可以用下面这一版：

### 第3章 基于深度学习的房颤检测算法设计

3.1 算法整体框架设计  
3.2 基于RR间期的规则检测与自动标注方法  
3.3 面向低标注场景的CNN-LSTM协同建模方法  
3.4 基于交互式伪标签的迭代优化方法  
3.5 本章小结

这一版更偏“方法设计”风格，适合论文整体语言比较正式、希望减少工程实现感的写法。

## 五、各节应该写什么

为了方便你后面继续写正文，下面把 3.3 和 3.4 的写作重点也一起整理出来。

### 3.3 CNN-LSTM双模型协同检测模型设计

这一节建议重点写以下内容：

1. 为什么不只使用单一 CNN 或单一 LSTM，而是采用双模型协同。
2. CNN 分支负责提取局部形态特征，如 QRS 波群和波形局部变化。
3. LSTM 分支负责建模时序依赖关系，捕获节律变化和上下文信息。
4. 两类模型在 ECG 房颤检测任务中的互补性。
5. 最终输出层如何完成二分类概率预测。

这一节的核心不是讲训练，而是讲“模型为什么这样设计、每个模块负责什么”。

### 3.4 交互式伪标签迭代训练策略

这一节建议重点写以下内容：

1. 在低标注场景下，为什么需要伪标签训练。
2. 规则方法先生成第一批高可信伪标签，用于模型冷启动。
3. CNN 和 LSTM 在每轮训练后分别对未标注样本进行预测。
4. 通过高置信度阈值筛选可靠样本，并进行类别平衡控制。
5. 通过双模型交互机制实现样本交叉馈送，逐轮更新训练集。
6. 通过多轮迭代不断提升模型性能，并结合集成结果选取最优轮次。

这一节的核心是讲“模型如何训练得越来越好”，而不是讲模型结构本身。

## 六、最建议你直接采用的目录版本

如果你现在要尽快定稿，我建议你直接使用下面这一版：

### 第3章 基于深度学习的房颤检测算法设计

3.1 算法整体框架设计  
3.2 基于RR间期的规则检测与自动标注方法  
3.3 CNN-LSTM双模型协同检测模型设计  
3.4 交互式伪标签迭代训练策略  
3.5 本章小结

这版最稳，原因是：

1. 与你的代码完全一致。
2. 章节职责划分清楚。
3. 容易往下写，不会出现内容交叉。
4. 容易通过答辩老师对“逻辑是否清楚”的检查。

## 七、后续写作建议

你后面写第 3 章时，可以按下面这个逻辑推进：

1. 3.1 写整体设计思路和模块关系。
2. 3.2 写规则冷启动和 RR 间期自动标注。
3. 3.3 写 CNN 与 LSTM 的结构设计和协同原因。
4. 3.4 写伪标签筛选、双模型交互和迭代训练机制。
5. 3.5 对本章进行总结，并为第 4 章实验部分做铺垫。

如果继续往下写，下一步最自然的是直接补 3.3 和 3.4 的正文内容。