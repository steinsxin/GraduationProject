# 4.3 评价指标与实验方法

为了全面评估本文所提出房颤检测方法的有效性，本节从评价指标和实验方法两个方面进行说明。其中，评价指标用于衡量模型在分类任务中的性能表现，实验方法用于说明本文在 CPSC2025 和 MIT-BIH 两个数据集上如何组织实验、如何设置对比方案以及如何验证第 3 章中所提出关键策略的作用。

## 4.3.1 评价指标

本文将房颤检测任务视为二分类问题，因此实验中主要采用准确率、精确率、召回率和 F1 值作为评价指标。这些指标能够从不同角度反映模型的检测性能，既能体现总体分类正确程度，也能衡量模型对异常样本的识别能力。

在以下公式中，$TP$ 表示将房颤样本正确判定为房颤的数量，$TN$ 表示将正常样本正确判定为正常的数量，$FP$ 表示将正常样本误判为房颤的数量，$FN$ 表示将房颤样本误判为正常的数量。

### 1. 准确率

准确率用于表示模型预测正确的样本数占总样本数的比例，是最直观的分类性能指标。该指标能够反映模型整体判别能力，适用于对总体分类效果进行初步衡量。

其计算公式为：

$$
Accuracy = \frac{TP + TN}{TP + TN + FP + FN}
$$

### 2. 精确率

精确率表示被模型判定为房颤类的样本中，真正属于房颤类的比例。该指标主要反映模型误报情况。精确率越高，说明模型将正常样本误判为房颤样本的比例越低。

其计算公式为：

$$
Precision = \frac{TP}{TP + FP}
$$

### 3. 召回率

召回率表示所有真实房颤样本中，被模型成功识别出的比例。该指标主要反映模型漏检情况。对于房颤检测任务而言，召回率具有重要意义，因为漏检异常样本可能直接影响后续临床辅助判断的可靠性。

其计算公式为：

$$
Recall = \frac{TP}{TP + FN}
$$

### 4. F1 值

F1 值是精确率和召回率的综合指标，能够在误报与漏报之间取得平衡。由于房颤检测任务中仅使用单一指标往往难以全面反映模型性能，因此本文将 F1 值作为重要参考指标之一，用于综合评价模型的整体检测效果。

其计算公式为：

$$
F1 = \frac{2 \times Precision \times Recall}{Precision + Recall}
$$

总体而言，准确率用于衡量模型整体正确率，精确率和召回率分别反映误报和漏报情况，F1 值则用于综合评价模型性能。多指标联合使用，能够更全面地分析本文方法在不同实验条件下的表现。

## 4.3.2 主实验方法

本文的主实验基于 CPSC2025 数据集开展，目标是验证第 3 章提出的房颤检测方法在主要研究对象上的整体有效性。实验流程与第 3 章提出的方法保持一致，主要包括规则冷启动、双模型协同训练和交互式伪标签迭代优化三个阶段。

具体来说，首先利用基于 RR 间期的规则检测方法对未标注样本进行自动标注，筛选出高置信度样本作为初始伪标签训练集；随后，构建 CNN 和 LSTM 两个异构模型，并使用该初始训练集完成第一轮训练；之后，再利用两个模型对未标注数据进行高置信度预测和交叉馈送，在多轮迭代中逐步更新训练集，最终得到检测性能更优的模型。

在主实验中，本文重点关注以下内容：

1. 所提方法在 CPSC2025 数据集上的整体检测性能。
2. 双模型协同结构相较于单模型方法是否具有性能优势。
3. 交互式伪标签迭代训练策略是否能够持续提升模型效果。

主实验结果将作为后续实验分析的核心内容，用于说明本文方法在目标任务上的应用价值。

## 4.3.3 辅助验证实验方法

除主实验外，本文还在 MIT-BIH 数据集上开展辅助验证实验，其目的不是重复完成与主实验完全相同的研究任务，而是重点验证第 3 章提出的交互式伪标签迭代训练策略是否确实有效。

在该部分实验中，本文重点比较以下两类方法：

1. 单模型自训练方法，即仅使用 CNN 模型在伪标签迭代过程中进行自训练。
2. 双模型交互迭代方法，即同时使用 CNN 和 LSTM 两个模型进行训练，并通过模型间交叉馈送不断更新训练集。

通过在 MIT-BIH 数据集上对两种方法进行对比，可以更清楚地观察：在不同数据来源和记录级划分条件下，交互式迭代策略是否仍然能够带来性能提升。如果结果表明双模型方法优于单模型方法，则说明本文策略不仅适用于主实验数据集，也具有一定普适性和可迁移性。

因此，MIT-BIH 实验在本文中的作用更偏向“策略验证”，用于支撑本文提出训练机制的合理性，而不是替代 CPSC2025 的主体实验地位。

## 4.3.4 对比实验方法

为了验证本文方法的有效性，实验部分设置了对比实验。对比实验的核心思想是，在尽可能保持其他条件一致的前提下，仅改变模型结构或训练方式，从而分析不同方法之间的性能差异。

结合本文研究内容，主要对比方法包括：

1. 基于单模型的自训练方法。
2. 基于双模型的交互式伪标签迭代训练方法。

其中，单模型方法可以看作本文方法的基线方案，其训练过程仍然依赖高置信度伪标签扩充训练集，但缺少异构模型之间的信息交互；双模型方法则进一步引入 CNN 与 LSTM 的协同学习机制，使模型能够在迭代训练中利用来自另一模型的补充信息。

通过这种对比方式，可以更加明确地说明：模型性能提升究竟来自普通伪标签迭代本身，还是来自本文提出的双模型交互机制。这一比较对于支撑本文方法创新点具有重要作用。

## 4.3.5 消融实验思路

除对比实验外，本文还可通过消融分析进一步观察各关键模块对整体性能的影响。所谓消融实验，是指在完整方法基础上去掉某一模块或改变某一策略，再比较性能变化，从而验证该模块的贡献。

结合第 3 章方法设计，本文方法中可重点分析的模块包括：

1. 基于 RR 间期的规则冷启动机制。
2. CNN-LSTM 双模型协同结构。
3. 高置信度伪标签筛选机制。
4. 交互式交叉馈送迭代训练策略。

若后续实验结果表明，当去除某一模块后模型性能出现明显下降，则可以说明该模块对整体检测效果具有积极贡献。通过这种方式，可以更细致地分析本文方法各组成部分的实际作用。

## 4.3.6 实验结果记录方式

为了保证后续结果分析具有完整依据，本文在实验中记录了多轮训练过程中的关键指标，包括单模型和集成模型在测试集上的准确率、F1 值以及其他相关评价指标。同时，对于交互式训练过程，还记录不同轮次的训练结果变化，以便观察模型在多轮伪标签迭代过程中的性能演化趋势。

这种记录方式有助于从两个层面分析本文方法：

1. 比较不同方法最终性能高低。
2. 分析模型在迭代训练过程中的提升规律。

前者用于说明本文方法是否优于基线方案，后者用于说明第 3 章提出的交互式伪标签迭代训练策略是否真正发挥了作用。

## 4.3.7 本节小结

本节从评价指标和实验方法两个方面对实验设计进行了说明。评价指标部分采用准确率、精确率、召回率和 F1 值，从总体性能与异常样本识别能力两个层面衡量模型效果；实验方法部分则分别介绍了基于 CPSC2025 的主实验、基于 MIT-BIH 的辅助验证实验、单模型与双模型的对比实验以及关键模块的消融分析思路。上述实验设计与第 3 章提出的方法框架保持一致，为后续实验结果与分析提供了明确依据。