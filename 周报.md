# 周报

## 本周工作内容

本周的主要工作是将之前在其他数据集上已经验证有效的“基于伪标签的迭代学习”方法迁移到 MIT-BIH 数据集上，并对单模型自训练方法和双模型交互迭代方法进行对比验证。

当前任务的整体流程为：

1. 从训练集中抽取少量有标签数据作为初始监督信息。
2. 使用初始标注数据对模型进行预训练。
3. 在大量无标签数据上进行伪标签预测。
4. 将高置信度伪标签样本加入训练集，进行多轮迭代更新。
5. 对比单模型自训练与双模型交互迭代两种方法在当前数据集上的表现差异。

我的核心观点是：单模型在伪标签迭代过程中容易受到自身偏差限制，而双模型交互迭代可以利用不同模型的特征提取能力进行互补，从而学习到更丰富的特征表示，理论上应优于单模型方案。

## 本周针对 MIT-BIH 数据集所做的调整

在迁移到 MIT-BIH 数据集的过程中，针对模型和伪标签策略做了两项关键改动：

### 1. 调整 LSTM 模型结构

在初始迁移实验中，LSTM 模型训练效果较差，无法稳定提供高质量伪标签，也难以在交互训练中发挥应有作用。因此本周对 LSTM 的模型结构进行了修改，希望提升其时序特征建模能力和训练稳定性。

### 2. 放宽伪标签筛选条件

考虑到 MIT-BIH 数据集上的模型置信度分布与之前的数据集存在差异，若继续沿用原有较严格的筛选条件，能够进入下一轮训练的伪标签样本数量不足，难以支撑持续迭代。因此本周适当放宽了伪标签筛选阈值，以增加可利用样本数量，提高迭代训练的推进能力。

## 实验设计

本周主要围绕以下两种方法进行实验验证：

### 单模型方法

仅使用 CNN 模型进行自训练。流程为：使用少量真实标签训练初始 CNN，然后在无标签数据上筛选高置信度伪标签，逐轮加入训练集继续训练。

### 双模型交互迭代方法

同时使用 CNN 和 LSTM 两个模型进行训练，并通过模型间的交互机制进行伪标签扩充。整体思路是：

1. CNN 和 LSTM 分别基于当前训练集独立训练。
2. CNN 先从无标签数据中筛出一批高置信度样本作为共享伪标签。
3. 之后结合 CNN 与 LSTM 对剩余无标签样本的预测结果进行交互筛选。
4. 将交互得到的伪标签分别反馈给对应模型，继续下一轮训练。

这种方式的目标是利用 CNN 的局部形态特征提取能力与 LSTM 的时序建模能力形成互补，提高整体伪标签质量和特征学习效果。

## 目前实验结果

当前在 MIT-BIH 数据集上的实验结果如下：

| 方法 | 指标 | 结果 |
| --- | --- | --- |
| 双模型交互迭代 | CNN 测试集准确率 | 0.934 |
| 双模型交互迭代 | LSTM 测试集准确率 | 0.803 |
| 双模型交互迭代 | 集成测试集准确率 | 0.9095 |
| 单模型自训练 | CNN 测试集准确率 | 0.9005 |

从当前结果可以看出：

1. 在双模型交互迭代框架下，CNN 的测试准确率达到 0.934，高于单模型自训练的 0.9005，说明交互机制对 CNN 分支有明显促进作用。
2. 与单模型相比，双模型方法至少在 CNN 子模型表现上体现出一定提升，说明引入交互学习后，模型能够从更多样的伪标签信息中受益。
3. 目前 LSTM 分支效果仍然偏弱，测试准确率仅为 0.803，说明虽然已经对结构进行了调整，但其性能仍然不足以与 CNN 形成更强的双向互补。
4. 当前集成结果为 0.9095，虽然优于单模型结果，但尚未完全发挥出“两个强模型交互后显著优于单模型”的理想效果，后续仍有优化空间。

## 结果分析

结合当前实验现象，初步分析如下：

### 1. 双模型交互策略是有效的

从 CNN 指标提升可以看出，交互迭代并不是无效设计。即使 LSTM 当前性能较弱，它仍然在一定程度上参与了伪标签筛选过程，对 CNN 的迭代训练产生了正向影响。这说明“多模型交互学习优于单模型闭环学习”的思路在该数据集上仍然具备可行性。

### 2. 当前瓶颈主要在 LSTM 分支

双模型方法的上限取决于参与交互的两个模型是否都具有足够可靠的判别能力。当前 LSTM 效果偏弱，会限制交互伪标签的质量，也会削弱最终集成性能。因此目前的主要问题并不是交互框架本身，而是第二个模型的有效性仍需进一步提升。

### 3. 放宽伪标签条件提升了迭代可执行性，但也带来噪声风险

放宽伪标签筛选条件后，确实可以让更多无标签样本进入后续训练，缓解样本不足问题，但同时也可能引入一定噪声标签。当前 CNN 性能仍然获得提升，说明这项调整整体上是有效的；但如果要进一步提升整体性能，还需要在“样本数量”和“伪标签质量”之间继续寻找更合适的平衡点。

## 本周结论

本周已经完成了交互迭代方法在 MIT-BIH 数据集上的初步迁移和验证。实验结果表明：

1. 单模型自训练在该任务上能够取得一定效果，但性能存在上限。
2. 双模型交互迭代方法能够提升 CNN 分支的测试表现，证明该思路在当前数据集上仍然有效。
3. 当前方法的主要限制在于 LSTM 分支性能不足，导致双模型互补优势尚未完全释放。
4. 下一阶段工作的重点应放在继续优化 LSTM 结构与交互筛选机制上，从而进一步提升整体交互学习效果。

## 下周计划

1. 继续优化 LSTM 模型结构，提升其在 MIT-BIH 数据集上的基础分类性能。
2. 进一步分析不同伪标签阈值设置对交互训练结果的影响，寻找更合适的筛选策略。
3. 对交互过程中不同轮次的伪标签数量和质量进行统计分析，评估每一轮交互带来的真实收益。
4. 尝试改进双模型之间的交互方式，降低弱模型对整体性能的负面影响。
5. 在更多随机种子或更稳定的实验设置下重复实验，验证当前结论是否具有一致性。