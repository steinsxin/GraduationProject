# 4.2 实验环境与参数配置

为了保证实验过程的可复现性，并便于对本文方法在不同数据集上的性能进行统一分析，本节从实验环境和参数配置两个方面对实验设置进行说明。结合第 3 章提出的方法，本文实验既包括基于 CPSC2025 数据集的主体实验，也包括基于 MIT-BIH 数据集的辅助验证实验，因此参数设置在保持整体一致性的同时，也根据不同数据集特点进行了适当调整。

## 4.2.1 实验环境

本文实验在 Windows 操作系统下完成，开发环境采用 Conda 虚拟环境进行管理，Python 版本为 3.9.25。为支持深度学习模型训练与信号处理流程，实验中主要使用了 PyTorch、NumPy、SciPy、scikit-learn、joblib、matplotlib、tqdm 等常用科学计算与机器学习库；在 MIT-BIH 数据集实验中，还使用了 wfdb 工具包完成原始心电记录与注释文件的读取。

在模型训练阶段，程序会自动检测当前设备是否支持 CUDA。若检测到可用 GPU，则优先调用 GPU 进行加速训练；若无可用 GPU，则回退到 CPU 环境运行。这样的设置使实验代码具有较好的适应性，也保证了本文方法在不同硬件条件下均可执行。

从软件组织形式上看，本文实验主要分为两个部分：

1. 基于 CPSC2025 数据集的交互式半监督训练实验。
2. 基于 MIT-BIH 数据集的交互式迭代策略验证实验。

虽然两个实验部分在数据来源和部分细节参数上有所区别，但在整体训练思想、模型结构和评价流程上保持一致，从而保证了实验结果之间具有可比性。

## 4.2.2 通用训练参数设置

在本文实验中，CNN 与 LSTM 两个模型均采用二分类任务设置，输出样本属于房颤类的概率值。训练过程主要使用二元交叉熵损失函数，优化器采用 Adam，批大小统一设置为 32，初始学习率设置为 0.001。这样的参数设置在实验中能够兼顾训练稳定性与收敛速度，也适合本文在低标注场景下开展多轮迭代训练。

在模型训练流程上，本文采用“当前训练集划分训练子集与验证子集，再保存验证集表现最优模型”的方式开展单轮训练。通过这种策略，可以降低小样本条件下模型过拟合的风险，提高后续伪标签筛选的可靠性。

此外，为增强模型对 ECG 波形扰动的适应能力，训练数据在加载阶段加入了轻量级数据增强操作，包括幅值缩放、随机噪声扰动、时间平移和局部掩码等。这些增强方式在不改变信号主要结构的前提下，提高了模型对不同波形变异和噪声环境的鲁棒性。

## 4.2.3 CPSC2025 数据集实验参数配置

在 CPSC2025 主实验中，本文主要采用第 3 章所提出的规则冷启动与双模型交互式迭代训练框架。相关核心参数设置如下：

1. 采样率设置为 400 Hz。
2. RR 间期规则检测中，CV 阈值设置为 0.02，ARI 阈值设置为 0.24。
3. 初始规则筛选阶段的严格置信度阈值设置为 85。
4. 训练批大小设置为 32。
5. 单轮训练轮数设置为 20。
6. 初始学习率设置为 0.001。
7. 交互式迭代总轮数设置为 10。
8. 高置信度伪标签筛选阈值设置为 0.95 和 0.05，分别对应房颤类与正常类候选样本。
9. 每类初始伪标签样本最多保留 2000 个，以控制训练规模并维持类别平衡。
10. 当前实验设置中随机重复次数设置为 1 次。

在数据使用方式上，CPSC2025 数据经过预处理后被划分为少量有标签数据和大量无标签数据。有标签样本主要用于最终测试，无标签样本主要用于规则自动标注和交互式伪标签迭代训练。单轮训练时，当前伪标签训练集再按照 8:2 的比例划分为训练集和验证集，以完成模型训练和最佳权重选择。

上述设置体现了本文在 CPSC2025 数据集上的核心实验思路，即利用少量真实标签隔离测试，大量未标注数据参与规则标注与模型迭代训练，从而模拟真实低标注场景下的应用条件。

## 4.2.4 MIT-BIH 数据集实验参数配置

在 MIT-BIH 辅助验证实验中，本文重点验证交互式伪标签迭代策略是否具有稳定有效性。与 CPSC2025 主实验相比，MIT-BIH 数据集实验在保留总体训练框架的同时，对数据划分和样本筛选参数进行了更细化设置。

根据代码实现，MIT-BIH 实验的关键参数配置如下：

1. 输入心拍片段窗口长度设置为 720。
2. 初始有标签样本比例设置为 10%。
3. 训练批大小设置为 32。
4. 单轮训练轮数设置为 20。
5. 初始学习率设置为 0.001。
6. 总迭代轮数设置为 10。
7. 高置信度筛选阈值初始设置为 0.95 和 0.05。
8. 每类伪标签样本最大保留数量设置为 800。
9. 每轮最少新增伪标签样本数设置为 32，早期轮次放宽为 16。
10. 随机种子固定为 45，以保证实验结果可复现。
11. 高、低置信度阈值会随轮次动态调整，步长设置为 0.05。
12. LSTM 反向馈送给 CNN 的条件受性能门控约束，以减少弱模型带来的噪声传播。

在数据划分上，MIT-BIH 数据集采用按记录划分训练集、验证集和测试集的方法，避免同一条记录中的心拍样本同时出现在不同数据子集中，从而减小数据泄露风险。随后，再从训练集内部抽取少量样本作为初始标注数据，其余作为未标注数据参与后续半监督学习。

这一组参数设置与 CPSC2025 实验相比更加注重伪标签筛选稳定性和轮次控制，其目的在于更加清楚地观察双模型交互机制在真实记录级划分条件下的有效性。

## 4.2.5 参数设置依据分析

本文实验参数的选取主要依据以下几方面考虑：

1. 训练批大小和学习率采用较为常见且稳定的设置，以保证不同模型和不同数据集上的训练过程具有可比性。
2. 单轮训练轮数控制在适中范围内，以避免训练不足或过拟合，同时兼顾多轮迭代实验的计算成本。
3. 高置信度筛选阈值采用较严格的双阈值策略，以控制伪标签噪声，保证进入下一轮训练样本的可靠性。
4. 通过类别平衡和样本数量上限限制，降低某一类样本过多带来的分布偏移风险。
5. 在 MIT-BIH 数据集上进一步引入动态阈值与性能门控机制，是为了适应其记录级划分和更复杂的样本分布特征，提高交互式训练的稳定性。

总体而言，本文参数设置坚持“优先保证训练稳定性，再逐步提升未标注样本利用率”的原则。这与第 3 章提出的算法思想保持一致，也是本文实验能够顺利开展的重要前提。

## 4.2.6 本节小结

本节对本文实验所采用的运行环境和参数配置进行了说明。整体上，实验在 Windows 平台下基于 Python 3.9 和 PyTorch 框架实现，核心训练参数在不同实验中保持较高一致性；同时，针对 CPSC2025 主实验与 MIT-BIH 验证实验的不同任务目标，又分别设置了与数据特点相适配的参数方案。上述环境与参数配置为后续实验评价和结果分析提供了统一基础，也保证了本文实验过程具有较好的可复现性和合理性。