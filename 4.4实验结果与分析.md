# 4.4 实验结果与分析

为验证本文所提出房颤检测方法的有效性，本节分别从 CPSC2025 数据集上的主实验结果和 MIT-BIH 数据集上的辅助验证结果两个方面进行分析。其中，CPSC2025 数据集用于验证本文方法在主要研究对象上的整体性能，MIT-BIH 数据集用于进一步验证交互式伪标签迭代训练策略的有效性。通过双数据集实验，可以从应用效果和方法有效性两个层面支撑本文结论。

## 4.4.1 CPSC2025 数据集上的主实验结果

本文首先在 CPSC2025 数据集上对单模型监督学习方法和双模型半监督学习方法进行了对比实验，实验结果如表 4-1 所示。

| 方法 | 模型类型 | Accuracy | F1-Score |
| --- | --- | --- | --- |
| 单模型监督学习 | CNN | 0.9024 | 0.9079 |
| 双模型半监督学习 | CNN | 0.9400 | 0.9425 |
| 双模型半监督学习 | LSTM | 0.9390 | 0.9410 |
| 双模型半监督学习 | Ensemble（融合） | 0.9490 | 0.9515 |

从表 4-1 可以看出，本文提出的双模型半监督学习方法在 CPSC2025 数据集上取得了明显优于单模型监督学习的结果。

首先，在单模型监督学习设置下，CNN 的准确率为 0.9024，F1 值为 0.9079。该结果说明仅依靠单一模型和有限监督信息，虽然能够完成基本房颤检测任务，但模型性能仍然存在提升空间。

其次，在引入双模型半监督学习框架后，CNN 子模型的准确率提升至 0.9400，F1 值提升至 0.9425；LSTM 子模型的准确率和 F1 值也分别达到 0.9390 和 0.9410。与单模型监督学习结果相比，双模型半监督框架下两个子模型均表现出更优性能，说明本文提出的规则冷启动、双模型协同建模和伪标签迭代训练策略能够有效提升模型对房颤特征的学习能力。

最后，在对 CNN 和 LSTM 的输出结果进行融合后，集成模型取得了最优结果，其准确率达到 0.9490，F1 值达到 0.9515。与单模型监督学习相比，融合模型的准确率提高了 0.0466，F1 值提高了 0.0436；与双模型框架中的单个子模型相比，融合结果也进一步提升。这表明 CNN 与 LSTM 在特征表达上具有明显互补性，二者融合能够进一步提升分类稳定性和整体识别效果。

综合来看，CPSC2025 数据集上的实验结果充分说明，本文提出的双模型半监督学习方法相较于传统单模型监督学习方法具有明显优势。尤其是融合模型取得的最优性能，验证了第 3 章所提出“双模型协同 + 交互式伪标签迭代”技术路线在主要研究对象上的有效性。

## 4.4.2 MIT-BIH 数据集上的交互式迭代策略验证

除主实验外，本文还在 MIT-BIH 数据集上开展了辅助验证实验，重点比较单模型自训练方法与双模型交互迭代方法的性能差异，以验证交互式伪标签训练策略是否具有实际作用。实验结果如表 4-2 所示。

| 方法 | 指标 | 结果 |
| --- | --- | --- |
| 双模型交互迭代 | CNN 测试集准确率 | 0.9340 |
| 双模型交互迭代 | LSTM 测试集准确率 | 0.8030 |
| 双模型交互迭代 | 集成测试集准确率 | 0.9095 |
| 单模型自训练 | CNN 测试集准确率 | 0.9005 |

从表 4-2 可以看出，在 MIT-BIH 数据集上，双模型交互迭代框架下的 CNN 子模型测试准确率达到 0.9340，明显高于单模型自训练方法的 0.9005，准确率提升了 0.0335。这一结果说明，即使在辅助验证数据集上，交互式迭代训练策略仍然能够为主模型带来性能增益，证明第 3 章提出的双模型交互机制并非无效设计，而是具有实际应用价值。

进一步分析可以发现，MIT-BIH 数据集上的 LSTM 子模型测试准确率仅为 0.8030，明显低于 CNN 子模型。这说明在当前实验设置下，LSTM 分支虽然参与了交互式伪标签筛选，但其自身判别能力仍然偏弱，尚未与 CNN 形成足够强的双向互补关系。因此，当前双模型框架在 MIT-BIH 数据集上的性能提升，主要体现在 LSTM 对 CNN 训练过程的辅助促进作用，而不是两个强模型共同作用下的完全协同提升。

从集成结果看，双模型交互迭代框架的融合准确率为 0.9095。虽然该结果高于单模型自训练的 0.9005，但低于双模型框架中的 CNN 单模型结果 0.9340。这一现象说明，当参与融合的模型之一性能明显偏弱时，简单融合并不一定能够带来最优结果，反而可能在一定程度上削弱强模型的优势。换言之，双模型协同机制的上限不仅取决于交互式训练策略本身，也取决于参与交互的两个模型是否都具有足够可靠的判别能力。

尽管如此，MIT-BIH 实验仍然具有重要意义。因为该实验至少验证了一个关键结论，即交互式迭代策略能够在不同数据集上对主分支模型产生正向促进作用。这说明本文提出的训练机制具有一定的可迁移性和普适性，也为后续进一步优化 LSTM 分支结构和交互策略提供了依据。

## 4.4.3 结果综合分析

结合 CPSC2025 和 MIT-BIH 两组实验结果，可以对本文方法作出以下几点分析。

第一，本文提出的双模型半监督学习方法在主要研究对象 CPSC2025 数据集上表现出明显优势。无论是单个子模型还是融合模型，其准确率和 F1 值均优于单模型监督学习结果，说明第 3 章提出的整体技术路线是有效的。

第二，交互式伪标签迭代训练策略具有实际作用。MIT-BIH 数据集上的实验表明，在相同任务背景下，双模型交互迭代方法能够提升 CNN 分支性能，说明本文方法中的“模型交互”并不是形式上的设计，而是能够在训练过程中改善样本利用效率和特征学习质量。

第三，双模型协同机制的性能上限受到辅助模型质量影响。在 CPSC2025 数据集上，CNN 与 LSTM 两个分支都取得了较高性能，因此融合结果最优，体现出较强互补性；而在 MIT-BIH 数据集上，由于 LSTM 分支性能相对较弱，融合结果未能超过 CNN 单分支结果。这说明双模型方法虽然有效，但只有当两个子模型都具有较强基础判别能力时，协同优势才能被充分释放。

第四，本文方法更适用于“少量标注 + 大量未标注”的低标注场景。规则冷启动能够为模型提供可靠初始监督，双模型结构能够从不同角度提取 ECG 特征，而交互式伪标签训练则能够逐轮挖掘未标注数据中的有效信息。三者共同构成了本文方法性能提升的主要来源。

## 4.4.4 本节小结

本节分别从 CPSC2025 数据集上的主实验结果和 MIT-BIH 数据集上的辅助验证结果两个方面，对本文方法进行了分析。实验结果表明，本文提出的双模型半监督学习方法在 CPSC2025 数据集上取得了优于单模型监督学习的检测性能，验证了整体算法框架的有效性；同时，MIT-BIH 数据集上的对比实验也说明交互式伪标签迭代训练策略能够对主模型产生正向促进作用，验证了该策略的实际价值。

总体而言，实验结果与第 3 章的方法设计相一致，说明规则冷启动、双模型协同建模和交互式迭代训练三者相结合，能够有效提升低标注场景下的房颤检测性能。但同时实验也表明，双模型协同效果仍受辅助模型性能影响，后续若进一步优化 LSTM 分支结构与交互方式，模型整体性能仍有继续提升空间。