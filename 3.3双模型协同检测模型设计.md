# 3.3 双模型协同检测模型设计

## 3.3.1 设计思路

在完成基于 RR 间期的规则检测与自动标注之后，本文进一步引入深度学习模型对 ECG 信号进行自动特征学习，以弥补传统规则方法在复杂波形识别能力上的不足。考虑到房颤检测既依赖心电波形的局部形态信息，也依赖节律随时间变化的动态特征，本文没有采用单一模型进行识别，而是构建了由 CNN 和 LSTM 组成的双模型协同检测结构。

其中，CNN 分支主要负责提取 ECG 波形的局部空间特征，LSTM 分支主要负责建模信号序列中的时序依赖关系。两类模型分别从不同角度对输入心电片段进行表征，并在后续伪标签交互与结果融合中共同参与决策。通过这种方式，模型不仅能够学习到心拍的局部形态差异，还能够捕捉房颤节律中更深层次的时间变化规律。

因此，本文双模型协同检测结构的核心目标并不是简单地并联两个分类器，而是通过构建具有互补能力的异构模型，提高低标注场景下房颤检测的鲁棒性和泛化能力。

## 3.3.2 双模型总体结构

本文采用的双模型协同检测结构由 CNN 分支和 LSTM 分支两部分组成，二者接收相同的 ECG 片段输入，但各自采用不同的特征提取方式完成二分类预测。

从整体结构上看，该模型设计可以概括为以下过程：首先将预处理后的单导联 ECG 片段输入网络；随后，CNN 分支通过多层一维卷积逐级提取局部形态特征，LSTM 分支通过循环单元对时间序列变化进行建模；最后，两个模型分别输出样本属于房颤类的概率，用于后续的单模型评估、双模型交互和集成决策。

这种设计具有较强的任务针对性。对于房颤检测而言，单独依赖卷积网络，虽然能够较好识别局部波形异常，但对长程时间依赖的利用有限；单独依赖 LSTM，虽然能够描述节律变化，但对原始波形局部形态的抽取能力通常不如卷积网络稳定。因此，将二者结合起来，更符合 ECG 房颤检测任务“形态特征 + 节律特征”并重的特点。

## 3.3.3 CNN 分支设计

### 1. 设计目的

CNN 分支的主要任务是从原始 ECG 片段中自动提取具有判别性的局部波形特征。由于心电信号中的 QRS 波群、P 波和 T 波都具有明显的局部结构特征，而卷积神经网络在局部模式识别方面具有天然优势，因此本文首先引入一维卷积网络作为形态特征提取器。

### 2. 网络结构

根据代码实现，CNN 分支由四层一维卷积模块和一个全连接分类头组成。各卷积层依次完成特征提取、通道扩展和序列压缩，其基本结构为“卷积 + 批归一化 + ReLU 激活 + 池化”。

具体来看，第一层卷积采用较大的卷积核和步长，主要用于在较长时间窗口内快速提取宏观形态特征，并对输入序列进行初步降维。第二层卷积进一步在中等尺度上提取心电片段的中间层特征。第三层和第四层卷积则更加关注局部细节和深层抽象特征，使网络能够逐渐形成对异常波形模式的高层表示。

在卷积特征提取完成后，网络使用自适应平均池化将时间维度压缩为固定长度表示，再通过全连接层完成特征映射与二分类概率输出。分类头中引入 Dropout 操作，以降低模型在小样本条件下的过拟合风险。

### 3. 分支优势

CNN 分支具有以下几个方面的优势：

1. 能够自动学习 ECG 波形中的局部判别模式，减少对人工特征设计的依赖。
2. 对局部波形畸变、振幅变化和噪声扰动具有较好的鲁棒性。
3. 一维卷积计算效率较高，适合在多轮迭代训练中频繁使用。

因此，在本文整体方法中，CNN 分支不仅承担基础分类任务，也在后续交互式伪标签筛选中作为重要的教师模型来源。

## 3.3.4 LSTM 分支设计

### 1. 设计目的

与 CNN 更关注局部波形不同，LSTM 分支的核心任务是刻画 ECG 信号在时间维度上的动态变化规律。房颤的关键特征之一是节律的不规则性，这种特征往往体现在连续时间步之间的依赖关系中，因此仅依靠卷积网络提取局部片段特征仍然不够。为此，本文进一步引入 LSTM 模型，对输入信号的时序结构进行建模。

### 2. 网络结构

根据当前代码实现，LSTM 分支采用双向 LSTM 结构。输入 ECG 片段首先经过维度调整，由卷积网络常用的“通道在前”形式转换为 LSTM 所需的“时间步在前”序列表示，然后送入双向循环单元进行特征提取。

双向结构意味着模型在处理每个时间位置时，不仅考虑其之前的上下文信息，也考虑其之后的上下文信息。这种方式对于 ECG 片段分析尤其有意义，因为一个局部波形是否异常，往往需要结合前后时序变化共同判断。

在 LSTM 输出端，本文没有直接取单一时间步的结果，而是对整个时间序列的隐藏表示进行均值聚合，从而得到该样本的整体时序特征表示。随后，再通过全连接层和 Sigmoid 激活函数输出最终的二分类概率。

### 3. 分支优势

LSTM 分支的优势主要体现在以下几个方面：

1. 能够建模 ECG 片段中的时间依赖关系，更适合描述节律变化过程。
2. 双向结构可同时利用前向和后向信息，提高时序建模完整性。
3. 对房颤这类以节律紊乱为主要特征的任务具有较好的适配性。

因此，LSTM 分支在本文中承担的是“节律建模器”的角色，用于补充 CNN 在时序特征表达上的不足。

## 3.3.5 双模型协同机理

本文采用双模型结构的根本原因，在于 CNN 和 LSTM 在 ECG 信号分析任务中具有明显的互补性。

一方面，CNN 擅长从原始波形中提取局部形态特征，例如 QRS 波群宽度、波峰形状、局部振幅变化等信息；另一方面，LSTM 更擅长捕捉时间维度上的依赖关系，例如节律波动、前后心拍变化趋势以及整体时序稳定性等信息。对于房颤检测而言，这两类信息都具有重要价值，仅依赖其中某一种往往难以充分描述样本特征。

在本文框架中，双模型协同主要体现在以下三个层面：

1. 特征层面互补：CNN 提供局部形态描述，LSTM 提供时序动态描述。
2. 训练层面互补：两个模型在迭代过程中可利用彼此筛选出的高置信度样本更新训练集。
3. 决策层面互补：两个模型最终输出的概率结果可进行融合，以提升分类稳定性。

这种协同机制使得模型设计不再局限于单一结构，而是形成了从特征提取到决策输出的多层次互补框架，从而更适合复杂 ECG 房颤检测任务。

## 3.3.6 输入输出形式与分类目标

在本文模型中，输入数据为经过预处理后的单导联 ECG 片段。每个样本可以表示为固定长度的一维时间序列。为了适应不同网络结构的输入要求，CNN 分支直接接收带有单通道维度的信号张量，而 LSTM 分支则在前向传播时将其重新排列为时间序列输入形式。

两个模型的输出形式保持一致，均为 0 到 1 之间的概率值，用于表示当前样本属于房颤类的可能性。若输出概率大于设定阈值，则将样本判定为异常类；否则判定为正常类。统一的输出形式便于后续在伪标签生成、模型交互和集成决策中进行直接比较与融合。

从任务定义上看，本文将房颤检测视为二分类问题，即判断输入 ECG 片段属于正常类还是房颤类。这种定义能够与前述 RR 间期规则标注结果保持一致，也便于构建完整的半监督训练流程。

## 3.3.7 设计合理性分析

从算法设计角度看，本文双模型协同检测结构具有较强合理性。

首先，它符合 ECG 信号的多属性特征表达需求。心电信号既包含局部波形形态，又包含时间节律变化，采用单一结构难以同时充分建模这两类信息。CNN 与 LSTM 的组合能够较自然地覆盖这两种特征空间。

其次，它符合低标注场景下的模型训练需求。由于初始标注样本有限，单模型容易出现偏置学习或伪标签扩散误差，而双模型结构可以在后续迭代中相互提供补充信息，从而提高训练稳定性。

最后，它与本文整体半监督框架高度一致。3.2 节中的 RR 间期规则方法负责提供初始伪标签，3.3 节中的双模型结构负责学习更深层的波形和时序特征，后续 3.4 节中的交互式训练策略则负责让这两个模型在低标注条件下持续优化。因此，双模型协同检测结构在整篇论文中处于承上启下的位置，是连接规则方法与迭代训练机制的核心部分。

## 3.3.8 本节小结

本节围绕房颤检测任务的特征需求，设计了由 CNN 和 LSTM 构成的双模型协同检测结构。其中，CNN 分支主要负责提取 ECG 信号的局部形态特征，LSTM 分支主要负责建模节律变化的时序依赖关系。两类模型在特征表达、训练过程和决策输出上具有明显互补性，能够共同提高低标注场景下房颤检测的准确性与稳定性。

这一模型设计为后续交互式伪标签训练提供了基础。基于该结构，本文能够进一步构建双模型之间的样本交互和迭代优化机制，从而逐步提升未标注数据的利用效率和整体检测性能。