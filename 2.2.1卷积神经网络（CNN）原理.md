# 2.2.1 卷积神经网络（CNN）原理

卷积神经网络是一类专门处理具有网格结构数据的前馈神经网络，广泛应用于图像、语音和时间序列分析等任务。在心电信号处理场景中，ECG 属于典型的一维时间序列数据，相邻采样点之间具有较强相关性，因此卷积神经网络能够较好地利用其局部结构特征，对波形形态和节律模式进行自动建模。卷积神经网络的核心思想主要体现在局部连接、权重共享和层次化特征提取三个方面。

## 1. 卷积运算原理

卷积层通过一个可学习的卷积核在输入数据上滑动，对输入序列的局部区域进行加权求和，从而生成新的特征表示。对于一维心电信号，设输入序列为 $x$，长度为 $L$，卷积核为 $w$，大小为 $K$，偏置项为 $b$，则在位置 $i$ 处的卷积输出可表示为：

$$
y_i = \sum_{j=0}^{K-1} w_j x_{i+j} + b
$$

其中，$y_i$ 表示卷积层在位置 $i$ 处的输出，$w_j$ 表示卷积核第 $j$ 个权重参数，$x_{i+j}$ 表示输入序列对应位置的采样值。通过卷积运算，模型能够从输入信号中提取局部模式信息，并将其映射为新的特征图。

图 2-3 给出了一维卷积操作的基本示意。卷积核在输入信号上滑动时，每次只关注一个局部窗口，并将该局部区域映射为一个输出值，因此卷积层能够突出局部波形结构的重要信息。

```mermaid
flowchart LR
    X1[x_i] --> C((卷积核))
    X2[x_{i+1}] --> C
    X3[x_{i+2}] --> C
    X4[x_{i+3}] --> C
    B[偏置 b] --> C
    C --> Y[y_i]
```

图 2-3 一维卷积操作示意图

卷积操作具有两个重要特性。

其一是局部连接。每个输出单元只与输入信号中的一个局部区域相连，而不是与全部输入节点直接相连。这种方式符合心电信号分析中的先验认知，即相邻采样点之间往往具有更强的相关性，许多关键形态特征通常表现为局部波形变化，例如 QRS 波群的快速起伏、P 波的局部峰值变化以及 T 波的形态延展等。

其二是权重共享。同一个卷积核在整个输入序列上滑动时始终使用相同的参数，因此模型参数量显著减少。相比传统全连接网络，卷积网络在处理长序列时计算效率更高，同时这种共享机制也使模型具备一定的平移等变性，即同一种波形模式即使出现在不同时间位置，也能够被相同卷积核检测出来。

## 2. 池化层原理

在卷积层之后，通常会引入池化层对特征图进行下采样，以压缩特征维度并增强模型对局部扰动的鲁棒性。常见池化方式包括最大池化和平均池化。

设输入特征图的某一局部窗口为 $\{z_1, z_2, \dots, z_m\}$，则最大池化和平均池化分别可表示为：

$$
z_{max} = \max(z_1, z_2, \dots, z_m)
$$

$$
z_{avg} = \frac{1}{m} \sum_{k=1}^{m} z_k
$$

最大池化保留局部区域中最显著的激活响应，强调局部最强特征；平均池化则保留局部区域的整体平均信息，更注重平滑表达。在心电信号分析中，池化层能够使模型对轻微的时间偏移、波形幅值波动以及局部形变不那么敏感，从而提高模型稳定性。

## 3. CNN 的基本结构

卷积神经网络通常由输入层、卷积层、激活层、池化层、全连接层和输出层构成。对于 ECG 分析任务，其基本处理流程如图 2-4 所示。

```mermaid
flowchart LR
    A[输入一维ECG序列] --> B[卷积层]
    B --> C[激活函数 ReLU]
    C --> D[池化层]
    D --> E[卷积层]
    E --> F[激活函数 ReLU]
    F --> G[池化层]
    G --> H[展平 Flatten]
    H --> I[全连接层]
    I --> J[输出层]
```

图 2-4 卷积神经网络基本结构示意图

其中，卷积层负责提取局部特征，激活函数用于引入非线性表达能力，池化层用于压缩特征图并增强鲁棒性，全连接层则负责对提取到的高层特征进行整合，最终完成分类任务。在房颤检测中，输出层通常对应二分类结果，即正常心律或房颤心律。

## 4. 层次化特征提取机制

随着卷积网络层数加深，神经元的感受野会逐渐扩大。所谓感受野，是指某一层神经元在原始输入空间中所对应的区域范围。浅层神经元只能感知很小的局部区域，而深层神经元能够综合更大范围的信息，因此网络可以逐层构建由低级到高级的特征表达。

在 ECG 信号处理中，这种层次化特征提取机制具有明确意义。浅层卷积核通常学习局部边缘、波形斜率和局部振幅变化等底层特征；中层卷积核则在此基础上组合形成 P 波、QRS 波群和 T 波等结构性波形片段；深层卷积核进一步感知更大时间范围内的节律模式和高层语义信息。图 2-5 展示了这种层次化特征提取过程。

```mermaid
flowchart LR
    A[原始ECG信号] --> B[浅层卷积\n边缘 斜率 局部波形]
    B --> C[中层卷积\nP波 QRS波 T波片段]
    C --> D[深层卷积\n节律模式与高层语义特征]
    D --> E[全连接层/分类器\n正常或房颤]
```

图 2-5 CNN 层次化特征提取机制示意图

正是由于这种逐层抽象的能力，卷积神经网络能够自动学习从底层波形到高层节律的多级表示，从而减少对人工特征设计的依赖。

## 5. CNN 在心电信号分析中的优势

卷积神经网络在心电信号分析中具有以下几个方面的优势。

第一，参数共享机制使模型具有较高计算效率。由于同一卷积核在不同位置重复使用，模型参数量远少于全连接网络，因此更适合处理长时间序列数据，也更容易部署到资源受限的可穿戴设备和边缘计算平台。

第二，局部连接特性符合心电信号的结构先验。ECG 中的关键诊断信息往往体现在局部波形形态上，例如 QRS 波群宽度、P 波缺失或 T 波异常等，而卷积层天然适合捕捉此类局部模式。

第三，多层卷积堆叠能够提取多尺度特征。通过不同卷积核和不同层级组合，模型既可以捕获毫秒级的细粒度波形特征，也可以学习更长时间范围内的节律信息，从而提高对复杂异常模式的识别能力。

## 6. CNN 的局限性

尽管卷积神经网络在心电信号分析中具有明显优势，但其仍存在一定局限性。卷积网络的感受野主要受网络深度、卷积核大小和池化结构限制，当网络规模有限时，其对长程时序依赖的建模能力相对不足。例如，对于 RR 间期在较长时间尺度上的动态演变，仅依靠局部卷积结构往往难以充分刻画。

因此，在涉及更复杂节律建模的任务中，常常需要将 CNN 与 LSTM、Transformer 等时序建模结构结合使用，以同时利用局部波形特征和长程依赖信息。这也是本文后续在房颤检测模型中采用多模型协同结构的重要原因。

## 7. 本节小结

本节介绍了卷积神经网络的基本原理，包括卷积运算、池化层作用、网络基本结构以及层次化特征提取机制，并结合心电信号分析说明了 CNN 在房颤检测任务中的适用性。总体而言，卷积神经网络能够高效提取 ECG 的局部形态特征，并通过多层堆叠形成从底层波形到高层节律的多级表示，是心电自动分析中的重要模型基础。但由于其对长程时序依赖的建模能力有限，后续仍需要结合更强的时序建模方法，以进一步提升房颤检测性能。