# 3.4 交互式伪标签迭代训练策略

## 3.4.1 策略设计目标

在低标注场景下，深度学习模型通常面临两个突出问题：一是初始真实标注样本数量有限，难以支撑模型充分学习复杂 ECG 特征；二是如果直接将未标注样本的预测结果全部用于训练，又容易引入大量错误伪标签，导致模型性能下降。因此，如何在有限标注条件下稳步利用未标注样本，是整个半监督学习框架能否有效工作的关键。

针对这一问题，本文在双模型协同检测结构基础上，设计了一种交互式伪标签迭代训练策略。该策略以 RR 间期规则方法生成的初始伪标签集作为训练起点，在此基础上，利用 CNN 与 LSTM 两个模型对未标注样本进行反复预测、筛选和更新，使训练集在多轮迭代中不断扩充和优化。

与传统单模型自训练不同，本文方法并不是让单个模型独立完成“预测 - 选样 - 再训练”的闭环，而是通过双模型交互机制提升伪标签质量。换言之，一个模型不仅使用自身的训练结果，还会利用另一个模型提供的高置信度样本进行补充学习，从而形成逐轮增强的协同训练过程。

## 3.4.2 整体训练流程

本文交互式伪标签迭代训练策略可以概括为以下四个阶段：

1. 利用 3.2 节中的 RR 间期规则检测方法，从未标注数据中筛选出高置信度样本，构建初始伪标签训练集。
2. 以该初始训练集为基础，分别训练 CNN 和 LSTM 两个模型，并在测试集上评估当前轮次性能。
3. 使用两个模型对全部未标注样本进行预测，根据置信度阈值筛选可靠样本，并通过交叉馈送方式更新对方模型下一轮训练集。
4. 重复上述过程，直至达到设定迭代轮次，最终输出多轮训练中的最优结果。

该流程体现出一种典型的“冷启动 - 双模型训练 - 交互筛选 - 迭代更新”策略。其本质是在有限标注条件下，通过逐轮扩展高质量伪标签样本，逐步提高模型的判别能力。

## 3.4.3 初始伪标签训练集构建

在本文方法中，交互式迭代训练并不是从完全随机的状态开始，而是建立在规则方法提供的初始伪标签数据集之上。具体而言，3.2 节中通过 RR 间期特征和置信度筛选得到的高质量房颤样本与正常样本会被合并，并经过类别平衡处理后形成初始训练集。

这一设计具有重要意义。一方面，它为后续深度模型训练提供了可靠的起点，使模型在第一轮训练时就能够接触到较为典型的房颤和正常节律模式；另一方面，它避免了完全无监督条件下模型训练方向不稳定的问题。也就是说，传统规则方法在这里承担的是“启动监督”的作用，而深度学习模型则在其基础上继续挖掘更复杂的特征信息。

在你的代码实现中，CNN 和 LSTM 在第一轮训练时使用相同的初始伪标签数据集。这使两个模型拥有统一的起始样本分布，便于后续通过不同结构学习到互补特征，并在多轮训练中逐步分化出各自更擅长的判别模式。

## 3.4.4 单轮训练过程

在每一轮迭代中，算法首先分别训练 CNN 模型和 LSTM 模型。虽然两个模型结构不同，但它们遵循相同的训练流程，即根据当前轮次所对应的训练集进行监督学习，并输出本轮模型参数。

在当前代码实现中，每轮训练时都会先将当前伪标签数据划分为训练集和验证集，再通过二元交叉熵损失函数对模型参数进行优化。训练完成后，程序会保存验证表现最优的模型权重，并使用该最佳模型参与后续评估与伪标签生成。这种设计可以避免模型在单轮训练后期出现过拟合，从而提高后续伪标签筛选的可靠性。

由于 CNN 和 LSTM 对 ECG 特征的关注重点不同，同一轮训练后两个模型往往会对未标注样本形成不同的预测分布。这种差异并不是缺陷，反而是交互式训练能够发挥作用的前提，因为模型间的结构差异决定了它们在后续样本筛选过程中可以相互补充。

## 3.4.5 高置信度伪标签筛选机制

在每轮训练完成后，本文使用当前 CNN 和 LSTM 模型对未标注样本进行预测，并根据输出概率判断样本是否适合加入下一轮训练。为了控制伪标签误差，算法不会直接使用所有预测结果，而是仅保留高置信度样本。

在你的代码中，高置信度样本的筛选采用双阈值策略：

1. 当模型输出概率高于 0.95 时，将该样本视为高置信度房颤候选。
2. 当模型输出概率低于 0.05 时，将该样本视为高置信度正常候选。

这种策略的核心思想是，只选择模型判断最明确的样本进入下一轮训练。位于中间概率区间的样本通常具有更大的不确定性，如果贸然作为伪标签加入训练，容易将错误信息不断传递给后续模型。因此，高阈值与低阈值共同构成了伪标签质量控制的第一道屏障。

## 3.4.6 类别平衡与 Top-K 保留策略

仅依靠高置信度阈值进行筛选，仍然可能出现类别不平衡问题。例如，某一轮中模型可能更容易识别正常样本，导致正常类高置信度样本远多于房颤类样本。如果直接使用这些样本训练下一轮模型，训练集分布将发生偏移，进而影响模型对少数类的识别能力。

为解决这一问题，本文在高置信度筛选后进一步引入类别平衡与 Top-K 保留策略。具体而言，先分别统计房颤类和正常类高置信度样本数量，再取两类中的较小值作为本轮保留的目标数量。随后，在每类样本内部按照预测概率排序，仅保留置信度最高的一部分样本。

在你的代码中，这一策略既保证了进入下一轮训练的数据保持类别均衡，也避免了一次性加入过多样本而造成训练集剧烈波动。其本质是一种“宁缺毋滥”的伪标签扩充思想，即优先保证伪标签质量和类别结构，再考虑样本规模扩展。

## 3.4.7 双模型交叉馈送机制

本文交互式训练策略的核心创新点在于双模型交叉馈送机制。具体来说，在每一轮完成后，CNN 和 LSTM 并不是各自只使用自身筛选出的高置信度样本继续训练，而是将自己认为最可靠的样本提供给对方，作为下一轮训练集的一部分。

在你的代码实现中，这一过程可以分为两个方向：

1. CNN 为 LSTM 提供样本。即从 CNN 输出中筛选高置信度房颤样本和高置信度正常样本，经类别平衡处理后，作为下一轮 LSTM 的训练数据。
2. LSTM 为 CNN 提供样本。即从 LSTM 输出中筛选高置信度样本，经相同的平衡和排序操作后，作为下一轮 CNN 的训练数据。

这种设计的关键意义在于：两个模型并不完全依赖自身已有的判断，而是借助另一种结构视角补充训练信息。由于 CNN 更擅长局部形态分析，LSTM 更擅长时序依赖建模，因此一个模型所筛选出的高置信度样本，往往能够为另一个模型提供新的特征学习机会。

从训练机理上看，交叉馈送机制使双模型协同关系由“并行分类”进一步提升为“相互促进学习”。这也是本文方法区别于常规单模型伪标签学习的重要特征之一。

## 3.4.8 训练集动态更新方式

在完成高置信度筛选和交叉馈送后，算法将重新构建下一轮训练集。与固定训练集不同，本文的训练数据会随着迭代轮次不断变化。也就是说，模型每一轮面对的训练样本并不完全相同，而是由当前轮次筛选出的高质量伪标签动态决定。

在你的实现中，CNN 和 LSTM 各自维护独立的当前训练集。若某一模型在本轮成功为对方提供了足够数量的高置信度样本，则对方的训练集会在下一轮被替换为新的平衡伪标签集合；若未能筛选出足够可靠的样本，则保持原训练集不变。这种更新方式具有较强的稳定性，能够避免由于某一轮筛选失败而导致训练过程崩溃。

因此，本文训练集更新并不是简单累加所有历史伪标签，而是更偏向于根据当前模型状态选择最可靠的一批样本继续推进训练。这种策略能够在一定程度上控制错误标签长期累积的问题。

## 3.4.9 多轮迭代优化机制

本文将上述过程设置为多轮迭代训练。在你的代码中，总迭代轮次设为 10 轮。经过每一轮的模型训练、预测筛选和交叉馈送，训练集会不断被新的高质量伪标签样本刷新，从而推动模型逐步提升对未标注数据的识别能力。

多轮迭代的意义在于：第一轮模型虽然能够利用规则方法提供的初始伪标签进行训练，但其特征学习能力仍然有限；随着训练轮次增加，模型对未标注样本的判别能力逐渐增强，能够从更大范围的数据中识别出新的可靠样本；这些新样本又会反过来改善下一轮训练集质量，最终形成一种正向循环。

这种逐轮增强机制，是半监督学习框架中提高未标注样本利用率的核心手段，也使得本文算法能够在低标注场景下获得比单轮训练更好的性能表现。

## 3.4.10 集成评估与最优结果选择

为了评估每一轮交互式训练的实际效果，本文在每轮训练结束后都会对 CNN、LSTM 及二者集成模型进行测试。具体做法是分别计算两个单模型对测试集的预测结果，再通过平均概率融合形成集成输出，并据此计算分类指标。

这种评估方式有两方面作用：一方面可以观察单个模型在不同轮次中的性能变化，分析 CNN 和 LSTM 各自的优势；另一方面可以验证双模型融合后是否获得了更稳定的检测结果。由于两个模型在特征表达上具有互补性，集成结果往往比单一模型更稳健。

在你的实现中，程序会记录每轮的 CNN 准确率、LSTM 准确率和集成准确率，并最终保存所有轮次结果。这为后续实验分析提供了完整依据，也能够支持从多轮训练中选择表现最优的轮次作为最终结果。

## 3.4.11 该策略的优势分析

与传统单模型伪标签训练方法相比，本文交互式伪标签迭代训练策略具有以下优势：

1. 能够在少量标注样本条件下持续利用未标注数据，提高数据利用效率。
2. 通过高置信度筛选机制降低错误伪标签进入训练集的风险。
3. 通过类别平衡和 Top-K 保留策略缓解训练集分布偏移问题。
4. 通过双模型交叉馈送机制实现异构模型互补，增强样本筛选可靠性。
5. 通过多轮迭代逐步优化训练集质量和模型性能，提升整体检测效果。

因此，该策略不仅是一个简单的训练过程安排，更是本文整个半监督房颤检测方法中提升性能的关键环节。

## 3.4.12 本节小结

本节围绕低标注场景下未标注数据的有效利用问题，设计了交互式伪标签迭代训练策略。该策略以 RR 间期规则方法生成的初始伪标签作为冷启动基础，在每轮训练中通过高置信度筛选、类别平衡控制、双模型交叉馈送和动态训练集更新，不断提升 CNN 与 LSTM 两个模型的检测能力。

与单模型自训练方法相比，本文策略更强调异构模型之间的协同和互补，使伪标签生成与模型优化形成闭环过程。由此，未标注样本能够在多轮训练中被逐步转化为有效监督信息，为最终房颤检测性能的提升提供重要支撑。